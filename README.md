# Morrowly

Morrowly is a Flutter iOS app for shaping tomorrow with a lighter evening handoff, small focus windows, and reusable notes from recent days.

The initial Dart structure keeps product areas separated under `lib/journeys`, shared presentation pieces under `lib/shared`, and local seed content under `lib/fieldnotes`.
